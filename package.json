{
  "name": "agentops-reliability-lab",
  "version": "0.0.0",
  "private": true,
  "description": "LLM Reliability Lab monorepo for evaluating and observing AI agent and RAG workflows.",
  "packageManager": "pnpm@11.7.0",
  "engines": {
    "node": ">=24.0.0",
    "pnpm": ">=11.7.0"
  },
  "scripts": {
    "test": "pnpm --filter @llm-reliability-lab/api test",
    "typecheck": "pnpm --filter @llm-reliability-lab/api typecheck",
    "check": "pnpm typecheck && pnpm test"
  }
}
